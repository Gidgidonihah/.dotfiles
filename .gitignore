# Include ONLY my dotfiles
/*
!/.*

# Codeium... Another tool that doesn't use .config/
# Plus it stores the API key in the config file. Sure don't want that.
.codeium/

# Ingore env stuff
.env
env
.pyenv
.rubies
.frum
.nvm

# I override some git config settings for each machine, so don't commit those
.gitconfig.local.inc

# Don't want everyone seeing my private keys, now do I?
.ssh
.copilot-cli-access-token
.yarnrc.yml

# I don't need to back up history files
*history
*hist
*hst
.bash_sessions
.scdalias.zsh
.undodir*

# or info files
*shell
*info

# or vim spell files (don't remember saved words)
.vim/spell/

# And I don't use vscode
.vscode

# or cache files
*.pyc
.git-lint/
/.cache/
*.cache
.babel.json
.mypy_cache
.swp
*tmp*
.solargraph/cache
.yarn

# or any of this stuff
.wget-hsts
karabiner
*.espressostorage
*private*
/.CFUserTextEncoding
/.Codefresh/
/.DS_Store
/.MacOSX/
/.Trash/
/.autoenv_authorized*
/.aws/
/.bigqueryrc
/.bundle/
/.cfconfig
/.composer/
/.config/
/.cups/
/.docker/
/.dropbox/
/.electron/
/.fpp/
/.gem/
/.gemrc
/.gnupg*
/.grip
/.gsutil
/.helm
/.httpie/
/.ipython/
/.kbd
/.krew
/.kube
/.local/
/.m2
/.matplotlib/
/.minikube/
/.mono/
/.mycli.log
/.ngrok2/
/.node-*
/.npm*
/.openvpn-connect.json
/.oracle_jre_usage/
/.owlet
/.pearrc
/.pylint.d/
/.rbenv
/.rnd
/.shsh/
/.subversion
/.terraform.d/
/.tldrc/
/.virtualenvs/
/.yarnrc
/.zcompdump*
/.zoomus/
/.zsh-update
/.zsh_sessions/
